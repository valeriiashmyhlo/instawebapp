// import { createActions } from 'redux-actions';
// import * as api from "../../lib/api";

// export const LOAD_DATA = "LOAD_DATA";

// export const {
//   loadData
// } = createActions(
//   LOAD_DATA
// );

// export const fetchData = (query) =>
//   dispatch =>
//     api
//       .fetchData(query)
//       .then(data => dispatch(loadData(data)))
//       .catch(err => dispatch(loadData(err)))
